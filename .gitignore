# dependências e build
node_modules
dist

# ficheiros locais/temporários
*.local
.env
.DS_Store

# Cada ambiente recebe URL e chave publishable próprias. Não versionar `.env`
# evita que Preview/Test use acidentalmente o Supabase de produção.
# NUNCA commitar chaves sb_secret_... nem a connection string do Postgres.

# ferramentas de uso único (geraram o esquema.sql a partir das migrações antigas)
.scratch

# artefatos locais de auditoria (podem descrever o schema real)
artifacts/
